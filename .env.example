# Dán mã đo lường Google Analytics 4 vào đây rồi build lại để bật theo dõi.
# Lấy mã tại: Google Analytics -> Quản trị -> Luồng dữ liệu (Data stream) -> ID đo lường (dạng G-XXXXXXXXXX)
# Để trống = không bật GA (không tải script, không cookie).
PUBLIC_GA_ID=
